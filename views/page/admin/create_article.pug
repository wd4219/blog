extends nav
block right_content
  .container
    .article-category
      label(for="category") 分类：
      select(name="category")
        option(value="1") 1
        option(value="1") 2
        option(value="1") 3
        option(value="1") 4
    .article-label
      label(for="blog-label") 文章标签：
      input(type="text" id="blog-label" class="blog-label")
    .article-description
      label(for="blog-description") 文章摘要：
      textarea(id="blog-description" class="blog-description")
    .article-content
      label(for="blog-content") 文章内容：
      textarea(id="blog-content" class="blog-content")
    .submit-blog  提交博客